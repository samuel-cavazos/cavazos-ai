# 2026-03-19 Landing CTA Update

## What changed

- Removed top-right header buttons (`Home`, `Chat`) from `orb-chat-ui/index.html`.
- Replaced hero CTA `Open Chat` with `Login` and `Preview Orb` with `Info`.
- Updated landing copy to focus on client resource status, logs, and emergency alert configuration.
- Added a `Login` button overlay inside the orb preview panel to mirror the lower action zone from the chat composer layout.

## Stale documentation check

- Reviewed `orb-chat-ui/README.md`; route/file descriptions are still accurate and did not require changes.
- Existing wiki entries were left as historical notes and do not conflict with current route structure.
